plugins {
    id "com.android.library"
    id "kotlin-android"
}
applyAndroidLibraryModule(project)

android {
    namespace "app.dapk.state.android"
}

dependencies {
    implementation project(":common")
    api project(":state")

    implementation libs.compose.activity
}
